# packages/dev — Homepage services fragment.

- Dev:
    - code-server:
        icon: vscode.png
        href: https://code.{{HOMEPAGE_VAR_DOMAIN}}/
        description: VS Code in the browser
        server: my-docker
        container: code-server
    - Postgres:
        icon: postgres.png
        description: postgres:5432 (host :15432)
        server: my-docker
        container: postgres
    - Redis:
        icon: redis.png
        description: redis:6379 (host :16379)
        server: my-docker
        container: redis
