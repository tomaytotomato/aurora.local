# packages/git — Homepage services fragment.
# Merged into packages/core/homepage/config/services.yaml.

- Dev:
    - Forgejo:
        icon: forgejo.png
        href: https://git.{{HOMEPAGE_VAR_HOME_DOMAIN}}/
        description: Self-hosted git + CI
        server: my-docker
        container: forgejo
