# Notes

Silverbullet is a self-hosted notebook with live templates and a
full-text index.

The heading above matters to the test: the frontend strips the leading
`# …` line before rendering the body, so the served README must keep it.
