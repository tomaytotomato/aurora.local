<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.0</version>
    <relativePath/>
  </parent>

  <groupId>com.tomaytotomato.aurora</groupId>
  <artifactId>aurora-dashboard</artifactId>
  <version>0.1.0</version>
  <packaging>jar</packaging>
  <name>aurora-dashboard</name>
  <description>Aurora dashboard — admin plane for an aurora.local box</description>

  <properties>
    <java.version>25</java.version>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <docker-java.version>3.7.1</docker-java.version>
    <sqlite-jdbc.version>3.53.0.0</sqlite-jdbc.version>
    <flyway.version>13.0.0</flyway.version>
    <snakeyaml.version>2.6</snakeyaml.version>
    <argon2.version>2.11</argon2.version>
    <zxing.version>3.5.3</zxing.version>
    <testcontainers.version>1.21.3</testcontainers.version>
    <wiremock.version>3.9.1</wiremock.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>

    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>${sqlite-jdbc.version}</version>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
      <version>${flyway.version}</version>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>com.github.docker-java</groupId>
      <artifactId>docker-java-core</artifactId>
      <version>${docker-java.version}</version>
    </dependency>
    <dependency>
      <groupId>com.github.docker-java</groupId>
      <artifactId>docker-java-transport-httpclient5</artifactId>
      <version>${docker-java.version}</version>
    </dependency>

    <dependency>
      <groupId>org.yaml</groupId>
      <artifactId>snakeyaml</artifactId>
      <version>${snakeyaml.version}</version>
    </dependency>

    <dependency>
      <groupId>de.mkammerer</groupId>
      <artifactId>argon2-jvm</artifactId>
      <version>${argon2.version}</version>
      <optional>true</optional>
    </dependency>

    <!--
      QR encoding only (com.google.zxing:core), not the javase module —
      that pulls in an AWT-based image writer we don't need. VpnService
      builds the PNG bytes itself via BitMatrix + java.awt.image /
      ImageIO, both part of the JDK already.
    -->
    <dependency>
      <groupId>com.google.zxing</groupId>
      <artifactId>core</artifactId>
      <version>${zxing.version}</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!--
      TestContainers, for the handful of tests whose job is to check an
      assumption about a real dependency rather than our own logic. Tagged
      and excluded from the default run: see the surefire config below.
    -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!--
      Notifications domain: NotificationsService makes real outbound HTTP
      calls (ntfy / Discord webhook / generic webhook). WireMock stands in
      for those targets in tests rather than mocking HttpClient, so the
      whole request/response cycle — including a genuinely unreachable
      port and a genuine timeout — is exercised. wiremock-standalone
      bundles its own shaded Jetty + Jackson (avoids clashing with Spring
      Boot's own Jackson) and JUnit 5 support (WireMockExtension).
    -->
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
      <version>${wiremock.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>aurora</finalName>
    <plugins>
      <!--
        The default `mvn test` run stays fast: fakes only, no containers.
        Tests tagged `registry` need a container and a reachable registry,
        so they are excluded here and run by their own CI job with
        `-Dgroups=registry`. Two suites rather than one slow one.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <excludedGroups>registry</excludedGroups>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>com.tomaytotomato.aurora.AuroraApplication</mainClass>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
