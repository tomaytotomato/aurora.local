# Copy to inventory.ini and edit for your box(es).
# For single-host self-bootstrap use bootstrap.sh which passes --connection=local.

[home_servers]
aurora ansible_host=192.168.0.110 ansible_user=bruce

[home_servers:vars]
ansible_python_interpreter=/usr/bin/python3
